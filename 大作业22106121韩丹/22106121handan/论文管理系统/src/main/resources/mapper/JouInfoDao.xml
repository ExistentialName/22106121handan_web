<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tust.dao.JouInfoDao">

    <resultMap type="com.example.tust.entity.JouInfo" id="JouInfoMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="author" column="author" jdbcType="VARCHAR"/>
        <result property="abstracts" column="abstracts" jdbcType="VARCHAR"/>
        <result property="htmlCon" column="html_con" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="JouInfoMap">
        select
          id, title, author, abstracts, html_con
        from jou_info
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="JouInfoMap">
        select
          id, title, author, abstracts, html_con
        from jou_info
        <where>
            <if test="jouInfo.id != null">
                and id = #{jouInfo.id}
            </if>
            <if test="jouInfo.title != null and jouInfo.title != ''">
                and title = #{jouInfo.title}
            </if>
            <if test="jouInfo.author != null and jouInfo.author != ''">
                and author = #{jouInfo.author}
            </if>
            <if test="jouInfo.abstracts != null and jouInfo.abstracts != ''">
                and abstracts = #{jouInfo.abstracts}
            </if>
            <if test="jouInfo.htmlCon != null and jouInfo.htmlCon != ''">
                and html_con = #{jouInfo.htmlCon}
            </if>
        </where>
        limit #{pageIndex}, #{pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from jou_info
        <where>
            <if test="jouInfo.id != null">
                and id = #{jouInfo.id}
            </if>
            <if test="jouInfo.title != null and jouInfo.title != ''">
                and title = #{jouInfo.title}
            </if>
            <if test="jouInfo.author != null and jouInfo.author != ''">
                and author = #{jouInfo.author}
            </if>
            <if test="jouInfo.abstracts != null and jouInfo.abstracts != ''">
                and abstracts = #{jouInfo.abstracts}
            </if>
            <if test="jouInfo.htmlCon != null and jouInfo.htmlCon != ''">
                and html_con = #{jouInfo.htmlCon}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into jou_info(title, author, abstracts, html_con)
        values (#{jouInfo.title}, #{jouInfo.author}, #{jouInfo.abstracts}, #{jouInfo.htmlCon})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into jou_info(title, author, abstracts, html_con)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.title}, #{entity.author}, #{entity.abstracts}, #{entity.htmlCon})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into jou_info(title, author, abstracts, html_con)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.author}, #{entity.abstracts}, #{entity.htmlCon})
        </foreach>
        on duplicate key update
        title = values(title),
        author = values(author),
        abstracts = values(abstracts),
        html_con = values(html_con)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update jou_info
        <set>
            <if test="jouInfo.title != null and jouInfo.title != ''">
                title = #{jouInfo.title},
            </if>
            <if test="jouInfo.author != null and jouInfo.author != ''">
                author = #{jouInfo.author},
            </if>
            <if test="jouInfo.abstracts != null and jouInfo.abstracts != ''">
                abstracts = #{jouInfo.abstracts},
            </if>
            <if test="jouInfo.htmlCon != null and jouInfo.htmlCon != ''">
                html_con = #{jouInfo.htmlCon},
            </if>
        </set>
        where id = #{jouInfo.id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from jou_info where id = #{id}
    </delete>

</mapper>

